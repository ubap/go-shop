{
  "compilerOptions": {
    "target": "ESNext",
    "useDefineForClassFields": true,
    "module": "ESNext",
    /* This is the key setting that fixes your error. It tells TypeScript to resolve
       modules the way a modern bundler like Vite does. */
    "moduleResolution": "bundler",
    "strict": true,
    "sourceMap": true,
    "resolveJsonModule": true,
    "esModuleInterop": true,
    /* Vite handles the actual compilation/bundling, so we tell TypeScript
       not to generate any output files (.js files). Its job is just type checking. */
    "noEmit": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true
  },
  /* This tells TypeScript which files to include in its analysis. */
  "include": ["src", "vite.config.ts"]
}